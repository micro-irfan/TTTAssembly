// nf-test config — scaffolding only, see README.md "Testing".
// TODO(user): add fixture files under tests/data/ once stub BAM/FASTQ inputs are available.

config {
    testsDir "tests"
    workDir ".nf-test"
    configFile "nextflow.config"
    profile "test"
}
