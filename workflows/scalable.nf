// SCALABLE workflow — STUB.
// The user will supply the scalable-mode steps later (see CLAUDE.md §1). Do not invent them
// here; fail fast with an informative message instead.

workflow SCALABLE {
    error "scalable mode not yet implemented"
}
